spring.application.name=VolleyBallStatTracker

# --- JWT Security Properties (MANDATORY) ---
# FIX: The secret key for signing JWTs, loaded from the environment variable.
application.security.jwt.secret-key=${JWT_SECRET_KEY:secret-key-default-for-tests}
# FIX: The expiration time for JWTs in milliseconds (24 hours = 86400000 ms).
application.security.jwt.expiration=${JWT_EXPIRATION_MS:86400000} 
# --- Database configuration remains the same: ---
spring.datasource.url=jdbc:postgresql://db:5432/volleyball_db
spring.datasource.username=postgres
spring.datasource.password=Ninjazach7!1
spring.jpa.hibernate.ddl-auto=update

# --- ADD THESE LINES TO HANDLE CI ENVIRONMENT ---

# 1. Spring Profile Configuration
# Define the default profile to ensure the app starts even without explicit activation.
spring.profiles.default=local 

# 2. Test Exclusions for CI/CD
# This is crucial for fixing the 'Failed to load ApplicationContext' error.
# It tells Spring Boot to exclude key beans when the 'test' profile is active (as configured in the CI YAML).
spring.test.database.replace=none



Standard App Configuration (Keep your existing settings here)


server.port=8080

... other properties ...


SPRING SECURITY DEBUG CONFIGURATION (CRITICAL FIX)

Enables DEBUG logging specifically for Spring Security components.

This will show the Authorization decision process (Allow/Deny).

logging.level.org.springframework.security=DEBUG

Optional: If you want more detail on the Web filter chain

logging.level.org.springframework.security.web=DEBUG